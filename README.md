# music_database
Building a music database requires a structured approach to handle the relationships between artists, albums, tracks, and genres. To keep things efficient, we use normalization to ensure data isn't unnecessarily repeated.  Below is a foundational schema for a relational music database.
